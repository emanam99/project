// Konfigurasi versi aplikasi — ubah di sini untuk update di seluruh app
export const APP_VERSION = '1.0.0'

// Versi backend (API) — selaras dengan config.api_version di api/config.php
// Bisa diambil runtime dari GET /api/version
export const BACKEND_VERSION = '2.3.0'
