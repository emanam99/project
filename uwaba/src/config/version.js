// Konfigurasi versi aplikasi
// Ubah versi di sini dan akan otomatis terupdate di semua tempat yang menggunakan versi ini
export const APP_VERSION = '6.4.0'

// Versi backend (API) saat ini — harus sama dengan config.api_version di api/config.php
// Bisa juga diambil runtime dari GET /api/version
export const BACKEND_VERSION = '2.3.0'
