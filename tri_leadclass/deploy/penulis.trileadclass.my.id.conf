# penulis.trileadclass.my.id -> Docker container tri-leadclass (127.0.0.1:4325)
# Subdomain portal penulis (dashboard, kirim naskah, profil).
# TLS: certbot --nginx -d penulis.trileadclass.my.id

server {
    listen 80;
    listen [::]:80;
    server_name penulis.trileadclass.my.id;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:4325;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_read_timeout 60s;
    }
}
