/**
 * Halaman Kalender publik (tanpa login).
 * Menggunakan komponen yang sama dengan /kalender; API GET kalender & hari-penting bersifat publik.
 */
import KalenderPage from '../Kalender'

export default function PublicKalender() {
  return <KalenderPage />
}
