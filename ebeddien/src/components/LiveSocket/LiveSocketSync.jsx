/**
 * Presence sekarang ditangani di LiveSocketProvider (contexts/LiveSocketContext).
 * Komponen ini tetap di-render agar tidak mengubah pohon komponen; tidak melakukan apa-apa.
 */
export default function LiveSocketSync() {
  return null
}
