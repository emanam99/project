{
  "name": "wa",
  "version": "1.0.0",
  "description": "Backend WhatsApp via Baileys + auth via UWABA",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node scripts/check-wa-deps.cjs && node server.js",
    "dev": "node scripts/check-wa-deps.cjs && nodemon server.js",
    "clean": "node -e \"const fs=require('fs'),path=require('path'); const p=path.join(process.cwd(),'node_modules'); if(fs.existsSync(p)){ try { fs.rmSync(p,{recursive:true,maxRetries:3}); console.log('node_modules dihapus'); } catch(e){ console.error('Tutup semua proses Node/Chrome lalu coba lagi. Error:', e.message); process.exit(1); } }\"",
    "install:safe": "node scripts/npm-install-skip-chrome.cjs",
    "reinstall": "npm run clean && npm run install:safe",
    "ensure-browser": "node scripts/ensure-browser.cjs"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.21",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "link-preview-js": "^3.2.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
